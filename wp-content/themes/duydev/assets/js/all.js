jQuery(document).ready(function($) {
    // header height
    // var headerHeight = $('.header-top').innerHeight();
    // // console.log('headerHeight', headerHeight);
    // $('body').css('--header-height', headerHeight + 'px');

    // // click data-link="copy" and copy data-url to clipboard
    // $('[data-link="copy"]').click(function() {
    //     var url = $(this).data('url');
    //     navigator.clipboard.writeText(url);

    //     $('#cursor .copy-cursor .rounded-text .rounded-bg').text('Copied');
    //     setTimeout(function() {
    //         $('#cursor .copy-cursor .rounded-text .rounded-bg').text('Copy');
    //     }, 2000);
    // });
});